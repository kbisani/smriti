import 'package:flutter/material.dart';

class TimelinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Timeline & Graph')),
      body: Center(child: Text('Timeline and knowledge graph UI here')),
    );
  }
}
